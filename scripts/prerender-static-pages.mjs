/**
 * GitHub Pages has no server-side renderer. This deliberately small prerenderer
 * writes meaningful HTML for every public route after Vite builds the client
 * bundle. React then enhances the same URLs in the browser.
 */
import { mkdir, readFile, writeFile } from 'node:fs/promises'
import { blogs } from '../src/data/blogs.js'
import { categoryDescriptions, guides, resources, topics } from '../src/platform/content.js'

const root = new URL('../dist/', import.meta.url)
const site = 'https://devopsx.in'
const sourceIndex = await readFile(new URL('index.html', root), 'utf8')
const clientEntry = sourceIndex.match(/<script type="module"[^>]*src="[^"]+"[^>]*><\/script>/)?.[0] || ''
const stylesheets = [...sourceIndex.matchAll(/<link[^>]*rel="stylesheet"[^>]*>/g)].map((match) => match[0]).join('')
const clean = (value = '') => value.replace(/&lt;[^&]*&gt;/g, ' ').replace(/<[^>]*>/g, ' ').replace(/&amp;/g, '&').replace(/\s+/g, ' ').trim()
const escape = (value = '') => String(value).replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;')
const date = (value) => value ? new Intl.DateTimeFormat('en', { dateStyle: 'medium' }).format(new Date(value)) : 'DevOpsX field note'
const cards = (items) => `<div class="article-grid">${items.map((article) => `<article class="article-card"><div class="article-card-art"><span>${escape(article.category || 'DEVOPS')}</span></div><div class="article-card-body"><div class="article-meta"><span>${escape(article.readTime || '8 min read')}</span><span>${escape(date(article.publishedAt))}</span></div><h3>${escape(article.title)}</h3><p>${escape(clean(article.excerpt))}</p><a class="platform-text-link" href="/blog/${escape(article.slug)}">Read article</a></div></article>`).join('')}</div>`
const layout = (title, description, path, body, type = 'website', structuredData) => `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><link rel="icon" type="image/svg+xml" href="/favicon.svg"><meta name="robots" content="index, follow, max-image-preview:large"><meta name="description" content="${escape(description)}"><title>${escape(title)}</title><link rel="canonical" href="${site}${path}"><meta property="og:title" content="${escape(title)}"><meta property="og:description" content="${escape(description)}"><meta property="og:url" content="${site}${path}"><meta property="og:type" content="${type}"><meta property="og:site_name" content="DevOpsX"><meta name="twitter:card" content="summary"><meta name="twitter:title" content="${escape(title)}"><meta name="twitter:description" content="${escape(description)}">${stylesheets}${structuredData ? `<script type="application/ld+json">${JSON.stringify(structuredData)}</script>` : ''}</head><body><div id="root">${body}</div><script type="module" src="/assets/index.js"></script></body></html>`
// Vite fingerprints the entry JavaScript; reuse its emitted script tag instead of assuming a filename.
const withClient = (html, entry) => html.replace('<script type="module" src="/assets/index.js"></script>', entry)
const header = `<header class="platform-header"><div class="platform-shell platform-header-inner"><a class="platform-logo" href="/" aria-label="DevOpsX home">DevOps<span>X</span></a><nav class="platform-nav" aria-label="Primary navigation"><a href="/blog">Articles</a><a href="/resources">Resources</a><a href="/about">About</a><a href="/freelance">Freelance</a><a href="/search">Search</a></nav></div></header>`
const footer = `<footer class="platform-footer"><div class="platform-shell platform-footer-bottom"><span>© ${new Date().getFullYear()} DevOpsX. Built for better engineering.</span><span><a href="/privacy">Privacy</a><a href="/terms">Terms</a><a href="/sitemap.xml">Sitemap</a></span></div></footer>`
const shell = (body) => `${header}<main>${body}</main>${footer}`
const pageIntro = (eyebrow, heading, description) => `<section class="platform-shell page-intro"><span class="platform-eyebrow">${escape(eyebrow)}</span><h1>${heading}</h1><p>${escape(description)}</p></section>`

const write = async (path, html) => {
  const output = path === '/' ? new URL('index.html', root) : new URL(`.${path}/index.html`, root)
  if (path !== '/') await mkdir(new URL(`.${path}/`, root), { recursive: true })
  await writeFile(output, withClient(html, clientEntry))
}

const org = { '@context': 'https://schema.org', '@type': 'Organization', name: 'DevOpsX', url: `${site}/` }
await write('/', layout('DevOpsX | Reliable Systems. Better Engineering.', 'Practical DevOps, cloud, DevSecOps, Kubernetes, automation and AI engineering guides built from real-world engineering problems.', '/', shell(`<section class="platform-hero"><div class="platform-shell platform-hero-grid"><div class="platform-hero-copy"><span class="platform-eyebrow">DEVOPSX / ENGINEERING PLATFORM</span><h1>Reliable systems.<br><em>Better engineering.</em></h1><p>Practical DevOps, Cloud, DevSecOps, Kubernetes, Automation and AI engineering guides built from real-world engineering problems.</p><div class="platform-actions"><a class="platform-button" href="/blog">Explore Articles</a><a class="platform-button secondary" href="/resources">DevOps Resources</a></div></div></div></section><section class="platform-section featured-section"><div class="platform-shell"><div class="platform-section-heading"><div><span class="platform-eyebrow">FIELD NOTES</span><h2>Ideas worth putting <em>into production.</em></h2></div></div>${cards(blogs.slice(0, 6))}</div></section>`), 'website', [org, { '@context': 'https://schema.org', '@type': 'WebSite', name: 'DevOpsX', url: `${site}/` }]))

await write('/blog', layout('DevOps engineering articles | DevOpsX', 'Practical articles covering cloud infrastructure, Kubernetes, DevSecOps, automation and reliable delivery.', '/blog', shell(`${pageIntro('DEVOPSX / FIELD NOTES', 'Practical knowledge for <em>reliable systems.</em>', 'Technical articles across DevOps, DevSecOps, Kubernetes, cloud, automation and platform engineering.') }<section class="platform-shell archive-grid"><div>${cards(blogs)}</div></section>`)))

for (const [slug, title] of topics) {
  const description = categoryDescriptions[slug]
  const articles = blogs.filter((article) => `${article.title} ${article.category}`.toLowerCase().includes(slug.replace('-', ' '))).slice(0, 12)
  await write(`/${slug}`, layout(`${title} engineering guides | DevOpsX`, description, `/${slug}`, shell(`${pageIntro(`TOPIC / ${title.toUpperCase()}`, `${escape(title)} <em>engineering.</em>`, description)}<section class="platform-shell"><div class="platform-section-heading"><div><span class="platform-eyebrow">LATEST ARTICLES</span><h2>Production-focused <em>guides.</em></h2></div></div>${cards(articles.length ? articles : blogs.slice(0, 6))}</section>`)))
}

for (const article of blogs) {
  const description = clean(article.excerpt)
  const related = blogs.filter((item) => item.slug !== article.slug && item.category === article.category).slice(0, 3)
  const articleSchema = { '@context': 'https://schema.org', '@type': 'Article', headline: article.title, description, author: { '@type': 'Organization', name: 'DevOpsX' }, datePublished: article.publishedAt || '2026-01-01', dateModified: article.updatedAt || article.publishedAt || '2026-01-01', mainEntityOfPage: `${site}/blog/${article.slug}`, publisher: { '@type': 'Organization', name: 'DevOpsX', url: site } }
  await write(`/blog/${article.slug}`, layout(`${article.title} | DevOpsX`, description, `/blog/${article.slug}`, shell(`<article class="platform-shell article-layout"><div class="article-main"><nav class="breadcrumb" aria-label="Breadcrumb"><a href="/blog">Articles</a> / ${escape(article.category)}</nav><span class="platform-eyebrow">${escape(article.category)} / FIELD NOTE</span><h1>${escape(article.title)}</h1><p class="article-lede">${escape(description)}</p><div class="article-byline"><strong>DevOpsX Editorial</strong><span>Published ${escape(date(article.publishedAt))}</span><span>${escape(article.readTime || '8 min read')}</span></div><div class="article-content"><h2>Why this matters</h2>${(article.body || []).map((paragraph) => `<p>${escape(paragraph)}</p>`).join('')}<aside class="production-note"><strong>Production recommendation</strong><p>Make ownership, rollback paths and operational signals explicit before this pattern reaches production.</p></aside><h2>Production checklist</h2><ul><li>Define ownership, environments and rollback paths.</li><li>Automate repeatable checks and make failures visible.</li><li>Review security, cost and operational signals before release.</li></ul>${article.sourceUrl ? `<p><a class="platform-button" href="${escape(article.sourceUrl)}">Read the original source</a></p>` : ''}</div></div><aside class="article-aside"><nav class="toc" aria-label="On this page"><strong>On this page</strong><a href="#why">Why this matters</a><a href="#checklist">Production checklist</a></nav></aside></article><section class="platform-shell related-section"><div class="platform-section-heading"><div><span class="platform-eyebrow">KEEP READING</span><h2>Related <em>field notes.</em></h2></div></div>${cards(related)}</section>`), 'article', [articleSchema, org]))
}

const resourceRows = resources.map(([title, description, topic]) => `<a href="/${topic}"><strong>${escape(title)}</strong><small>${escape(description)}</small></a>`).join('')
await write('/resources', layout('DevOps resources and cheat sheets | DevOpsX', 'Command references, production checklists and practical resources for DevOps and cloud engineering.', '/resources', shell(`${pageIntro('DEVOPSX / RESOURCE CENTER', 'Tools for better <em>engineering.</em>', 'Searchable command references, checklists and practical starting points for daily engineering work.')}<section class="platform-shell resource-directory"><div class="resource-list">${resourceRows}</div></section>`)))
await write('/freelance', layout('DevOps & Cloud Engineering Services | DevOpsX', 'Practical Kubernetes, cloud, Terraform, DevSecOps and automation support for engineering teams.', '/freelance', shell(`${pageIntro('DEVOPSX / FREELANCE ENGINEERING', 'DevOps & Cloud <em>Engineering Services.</em>', 'Practical platform help for teams that need reliable infrastructure, safer delivery and clear operational ownership.')}<section class="platform-shell service-layout"><div class="service-list">${['Kubernetes / AKS', 'AWS / Azure', 'Terraform', 'CI/CD', 'DevSecOps', 'GitHub Automation', 'Observability', 'Cloud Cost Optimization', 'Infrastructure Automation'].map((service, index) => `<div><span>0${index + 1}</span><h2>${service}</h2></div>`).join('')}</div></section>`)))
await write('/about', layout('About DevOpsX | Engineering platform', 'DevOpsX is a technical engineering platform for modern cloud, Kubernetes and delivery systems.', '/about', shell(`${pageIntro('ABOUT DEVOPSX', 'Engineering with <em>less friction.</em>', 'DevOpsX is a technical engineering platform for people building, securing and operating modern software systems.')}<section class="platform-shell about-grid"><h2>Real-world systems. <em>Useful knowledge.</em></h2><p>DevOpsX focuses on practical engineering across cloud infrastructure, Kubernetes, DevSecOps, CI/CD, observability and automation. AI-assisted engineering belongs inside good operating practices: explicit constraints, reviewable changes, security controls and measurable outcomes.</p></section>`)))
await write('/search', layout('Search DevOpsX | Engineering library', 'Search the DevOpsX engineering library by article title, category and tag.', '/search', shell(`${pageIntro('DEVOPSX / SEARCH', 'Find the right <em>field note.</em>', 'Search titles, descriptions, categories and tags across the engineering library.')}<section class="platform-shell search-results"><p>Use the search field once the page is interactive.</p>${cards(blogs.slice(0, 6))}</section>`)))
for (const [path, title] of [['/privacy', 'Privacy policy'], ['/terms', 'Terms of use']]) await write(path, layout(`${title} | DevOpsX`, `${title} for DevOpsX.`, path, shell(`${pageIntro('DEVOPSX / LEGAL', title, 'This page will be completed with the policies applicable to your service and newsletter provider before data collection begins.')}`)))
await write('/404', layout('Page Not Found | DevOpsX', 'The requested DevOpsX page could not be found.', '/404', shell(`${pageIntro('404 / NOT FOUND', 'Page not in the <em>runbook.</em>', 'Search the engineering library or return to the homepage.')}<section class="platform-shell"><a class="platform-button" href="/">Back to home</a> <a class="platform-button secondary" href="/search">Search articles</a></section>`)))
await writeFile(new URL('404.html', root), await readFile(new URL('./404/index.html', root)))

console.log(`Prerendered ${blogs.length + topics.length + 9} static routes.`)
